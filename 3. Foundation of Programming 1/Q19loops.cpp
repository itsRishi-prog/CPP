// loop means "Round by round" and in other word "Itration";
#include<iostream>
using namespace std;
int main(){
    // write 10 times "Good morning"
    for(int i=1; i<=3; i++){
        cout<<"Goog morning"<<endl;
    }
}

    // here, "for" is a special keyword;
    // syntax of a loop:-
    //      for(initialisation; condition; increment){
    //          do something;
    //      }

    // steps :- at first it will "initialize", then "check" the condition, then goes "inside" the loop, then "increment" and at the end "repeat".

    // loop ke andar initialize krne pe sirf loop ke andr ki usko call ya use kr skte hai; loop ke bahar kaam hi krega.